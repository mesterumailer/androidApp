# SMS Manager for Android

مدیریت و تحلیل پیامک‌های دریافتی روی Android، با تمرکز فعلی روی **فاز ۱: Inbox، دسته‌بندی قابل تنظیم و استخراج اطلاعات مهم**.

> وضعیت فعلی: نسخه `0.7.0` — دریافت زنده پیامک و اعلان‌های دسته‌بندی‌شده

> **راهنمای ادامه توسعه برای Agentها:** برای شناخت دقیق وضعیت واقعی پروژه، معماری، تصمیم‌های قطعی، Known Issueها، وضعیت branch/PR و ترتیب ادامه کار، فایل **[PROJECT_HANDOFF.md](PROJECT_HANDOFF.md)** مرجع continuation است.
>
> **Baseline حفظ قابلیت‌های موجود:** [REGRESSION_BASELINE.md](REGRESSION_BASELINE.md)
>
> **دفتر بازخورد واقعی محصول:** [PRODUCT_FEEDBACK.md](PRODUCT_FEEDBACK.md)

## هدف پروژه

این پروژه در نسخه‌های اولیه قرار نیست جایگزین برنامه پیام‌رسان پیش‌فرض Android شود. تمرکز فعلی این است که بدون Default SMS App شدن:

- پیامک‌های دریافتی موجود در Inbox را بخوانیم.
- پیامک‌ها را نمایش و تحلیل کنیم.
- دسته‌بندی‌ها را با قواعد قابل تنظیم مدیریت کنیم.
- کاربر بتواند تعیین کند کدام دسته‌ها در Inbox نمایش داده شوند.
- کاربر بتواند پیام‌ها را از روی متن یا فرستنده جست‌وجو کند.
- اطلاعات مهم مثل OTP و مبلغ تراکنش را استخراج کنیم.

## روش توسعه از این نقطه

از این مرحله پروژه وارد **Improvement Phase** شده است. اولویت اصلی دیگر اضافه‌کردن سریع قابلیت‌های جدید نیست؛ بلکه حفظ رفتار فعلی، بررسی بازخورد استفاده واقعی، رفع دقیق مشکلات و بهبود UX/دقت بدون ایجاد regression است.

چرخه استاندارد هر تغییر:

```text
User Feedback / Observation
          |
          v
Reproduce
          |
          v
Root Cause
          |
          v
Minimal Safe Change
          |
          v
Unit / Regression Test
          |
          v
Device Test (when relevant)
          |
          v
Update Feedback + Docs
          |
          v
Close / Verify
```

**قاعده مهم:** هیچ قابلیت فعلی نباید صرفاً به‌دلیل refactor یا feature جدید حذف، تضعیف یا تغییر رفتاری داده شود مگر اینکه تصمیم محصولی صریحی ثبت شود.

## فاز ۱ — پیاده‌سازی فعلی

### قابلیت‌های موجود

- درخواست runtime permission برای `READ_SMS` و `RECEIVE_SMS`.
- خواندن پیامک‌های Inbox از `Telephony.Sms.Inbox`.
- نمایش و مدیریت سقف خواندن پیامک‌ها؛ مقدار پیش‌فرض ۱۰۰۰ پیام و بازه قابل تنظیم ۲۰ تا ۵۰۰۰ پیام.
- UI فارسی و RTL با هدر مدرن و Drawer سمت راست.
- انتخاب تم روشن یا تاریک از داخل تنظیمات برنامه با ذخیره ماندگار انتخاب کاربر؛ تم پیش‌فرض روشن است و انتخاب تم روی تمام صفحات برنامه اعمال می‌شود.
- زبان بصری یکپارچه با سطوح تونال، فاصله‌گذاری بازتر و بازخورد لمسی نرم برای تعاملات اصلی.
- تنظیم سقف خواندن Inbox و تعیین محدوده زمانی «تا امروز» یا «تا تاریخ مشخص».
- جست‌وجوی زنده در صفحه اصلی بر اساس بخشی از متن پیام یا نام/شماره فرستنده.
- دکمه پاک‌کردن جست‌وجو و نمایش تعداد نتایج.
- فیلتر نمایش دسته‌ها در یک بخش مجزای صفحه اصلی با امکان روشن/خاموش کردن هر دسته.
- وضعیت فعال/غیرفعال بودن هر دسته به‌صورت محلی ذخیره می‌شود و دسته‌های غیرفعال در Inbox نمایش داده نمی‌شوند.
- فیلتر مسدودسازی برای شماره/فرستنده و عبارت محتوایی قبل از Classification اجرا می‌شود و پیام‌های مسدودشده اصلاً برای دسته‌بندی یا اعلان وارد نمی‌شوند.
- دریافت پیامک جدید با `RECEIVE_SMS` انجام می‌شود و پیام پس از عبور از Block Filter و Classification، فقط در صورت فعال بودن اعلان همان دسته می‌تواند Notification بسازد.
- اعلان هر دسته مستقل است و کاربر می‌تواند یک صدای موجود روی گوشی را برای آن انتخاب کند؛ پیش‌فرض هر دسته بدون صداست، ویبره نداریم و میزان صدا از تنظیمات Notification خود Android پیروی می‌کند. تنظیم اعلان از فعال/غیرفعال بودن همان دسته برای نمایش و مدیریت Inbox مستقل است.
- برای Android 13 به بالا، مجوز `POST_NOTIFICATIONS` فقط هنگام فعال‌کردن اعلان یک دسته و در صورت نیاز درخواست می‌شود.
- وضعیت فعال‌سازی دسته‌ها از صفحه مدیریت دسته‌بندی‌ها قابل تغییر است.
- فیلترهای مسدودسازی از تنظیمات برنامه قابل افزودن و حذف هستند.
- امکان انتخاب یک یا چند پیام.
- کپی متن یک پیام یا چند پیام انتخاب‌شده.
- تغییر دستی دسته‌بندی هر پیام بدون تغییر متن واقعی SMS، با ذخیره ماندگار Override و امکان بازگشت به تشخیص خودکار.
- صفحه Settings برای ویرایش قواعد دسته‌بندی.
- ذخیره محلی تنظیمات در `SharedPreferences`.
- Ruleهای قابل تنظیم شامل:
  - عبارات فرستنده
  - کلمات الزامی
  - کلمات تشخیصی
  - کلمات ممنوع
  - حداقل تعداد کلمات تشخیصی
  - اولویت Rule
- تطبیق Ruleها با نرمال‌سازی فارسی/انگلیسی و تبدیل اعداد فارسی/عربی به انگلیسی.
- حل هم‌پوشانی Ruleها با امتیازدهی و priority.
- بازخوانی خودکار Inbox بعد از بازگشت از Settings.
- استخراج اولیه کدهای ۴ تا ۸ رقمی.
- استخراج اولیه مبلغ تراکنش.
- تست واحد برای matching، exclusion، threshold، جست‌وجو و فیلتر دسته‌ها.

### جست‌وجوی Inbox

جست‌وجو در همان صفحه اصلی انجام می‌شود و با هر تغییر متن، نتایج بلافاصله دوباره فیلتر می‌شوند. جست‌وجو روی دو منبع اعمال می‌شود:

1. متن کامل پیامک
2. نام یا شماره فرستنده

جست‌وجو قبل از مقایسه، نرمال‌سازی فارسی و تبدیل اعداد انجام می‌دهد تا اختلاف‌هایی مثل `ي/ی`، `ك/ک`، نیم‌فاصله و اعداد فارسی باعث از دست رفتن نتیجه نشوند.

جست‌وجو و فیلتر دسته‌بندی با هم ترکیب می‌شوند؛ بنابراین کاربر می‌تواند هم‌زمان یک عبارت را جست‌وجو کند و فقط دسته‌های موردنظر را نمایش دهد.

### ترتیب پردازش پیامک

```text
Telephony.Sms.Inbox
        |
        v
+---------------------+
| Block Filter        |
| sender / content    |
+---------------------+
        |
        v
+---------------------+
| Category Activation |
| active rules only   |
+---------------------+
        |
        v
+---------------------+
| SmsClassifier       |
| category / OTP /    |
| amount / confidence |
+---------------------+
        |
        v
| Inbox display filters |
| search + visibility  |
+---------------------+
```

پیام مسدودشده فقط از دید این برنامه کنار گذاشته می‌شود و SMS اصلی در دیتابیس سیستم Android حذف نمی‌شود.

## فیلتر نمایش دسته‌ها

در صفحه اصلی یک بخش مستقل با عنوان **فیلتر نمایش** وجود دارد. هر دسته با یک دکمه قابل روشن/خاموش شدن است و وضعیت انتخاب در `SharedPreferences` نگهداری می‌شود. دکمه **همه** همه دسته‌ها را دوباره فعال می‌کند.

این فیلتر فقط نمایش را تغییر می‌دهد؛ پیامک از Inbox حذف یا تغییر نمی‌کند.

## منطق Rule

یک پیام برای یک دسته زمانی match می‌شود که:

1. هیچ عبارت ممنوع آن Rule در متن نباشد.
2. تمام کلمات الزامی وجود داشته باشند.
3. حداقل تعداد مشخصی از کلمات تشخیصی پیدا شوند.
4. در صورت تعریف sender filter، وجود نشانه فرستنده به امتیاز Rule کمک می‌کند.
5. در صورت match چند Rule، Rule دارای امتیاز بالاتر انتخاب می‌شود و priority نقش tie-breaker دارد.

## معماری فعلی

```text
BaseActivity
   |
   +--> اعمال تم سراسری (روشن / تاریک)
   |
   +--> MainActivity
   |      |
   |      +--> Inbox / Search / Category Filters
   |      +--> SmsRepository
   |      +--> SmsInboxFilter
   |
   +--> AppSettingsActivity
   |      |
   |      +--> Theme (روشن / تاریک)
   |      +--> تنظیمات محدوده Inbox
   |
   +--> SettingsActivity
   |      |
   |      +--> Edit Rule
   |      +--> Save / Reset defaults
   |
   +--> PermissionSetupActivity
          |
          +--> READ_SMS permission flow

MainActivity
   |
   +--> SmsRepository
             |
             +--> Telephony.Sms.Inbox
             |
             +--> SmsClassifier
                       |
                       +--> SmsAnalysis
                              +--> Category
                              +--> OTP
                              +--> Amount
```

## ساختار پروژه

```text
.
├── app/
│   ├── src/main/AndroidManifest.xml
│   ├── src/main/java/com/mesterumailer/smsmanager/
│   │   ├── BaseActivity.kt
│   │   ├── MainActivity.kt
│   │   ├── SettingsActivity.kt
│   │   ├── AppSettingsActivity.kt
│   │   ├── PermissionSetupActivity.kt
│   │   ├── SmsReceiver.kt
│   │   ├── AppThemeManager.kt
│   │   ├── data/
│   │   ├── model/
│   │   ├── notification/
│   │   └── util/
│   └── src/test/java/com/mesterumailer/smsmanager/
├── .github/workflows/android.yml
├── .github/workflows/android-release.yml
├── build.gradle.kts
├── settings.gradle.kts
├── gradle.properties
├── README.md
├── PROJECT_HANDOFF.md
├── REGRESSION_BASELINE.md
└── PRODUCT_FEEDBACK.md
```

## نیازمندی‌ها

- Android Studio با Android SDK مناسب پروژه
- JDK 17
- Android API 35 برای compile/target
- حداقل Android API 26

## تست نسخه 0.7.0 روی گوشی

بعد از Build موفق نسخه 0.7.0، تست رفتاری این سناریوها انجام شود:

1. نصب APK و فعال‌سازی `READ_SMS` و `RECEIVE_SMS`.
2. بررسی Inbox و اطمینان از اینکه پیامک‌های موجود نمایش داده می‌شوند.
3. روشن‌کردن اعلان فقط برای یک دسته و ارسال یک SMS جدید مربوط به همان دسته.
4. بررسی عنوان، متن و دسته اعلان.
5. انتخاب صدای یک دسته و بررسی پخش آن از Notification volume خود Android.
6. بررسی اینکه اعلان بدون صدا واقعاً بی‌صدا و بدون ویبره است.
7. خاموش‌کردن اعلان یک دسته و اطمینان از اینکه SMS آن دسته اعلان ایجاد نمی‌کند.
8. افزودن sender/content به Block Filter و اطمینان از اینکه SMS مسدودشده اعلان ایجاد نمی‌کند.
9. لمس اعلان و بررسی بازشدن پیام مربوطه داخل برنامه.
10. ارسال چندقسمتی SMS و بررسی اینکه قطعه‌ها به یک پیام واحد تبدیل شوند.
11. دریافت دوباره همان Intent و بررسی جلوگیری از duplicate notification.

این مرحله «تست دستگاه واقعی» است و نتیجهٔ آن باید جدا از Unit Test و Build CI ثبت شود.

## تست خودکار با GitHub Actions

Workflow پروژه در `.github/workflows/android.yml` قرار دارد و برای push روی `main` و branchهای `feature/**` و همچنین Pull Request اجرا می‌شود.

Workflow این موارد را بررسی می‌کند:

- Checkout
- JDK 17
- Gradle
- `testDebugUnitTest`
- `assembleDebug`
- تولید APK
- upload کردن APK به‌عنوان artifact

## اعلان‌ها

دریافت زنده پیامک با `SMS_RECEIVED_ACTION` برای برنامه‌های غیر Default SMS نیز در Android قابل استفاده است و برای آن `RECEIVE_SMS` لازم است. اعلان‌ها روی Notification Channelهای جداگانه برای هر دسته قرار می‌گیرند و Android 13+ به مجوز `POST_NOTIFICATIONS` نیاز دارد.

انتخاب صدا با Ringtone Picker سیستم انجام می‌شود؛ فایل صوتی جدیدی داخل APK قرار نمی‌گیرد. صدای Channel پیش از ثبت آن تعیین می‌شود، بنابراین تغییر صدا از داخل برنامه با بازسازی Channel همان دسته اعمال می‌شود.

## محدودیت‌های فعلی اعلان

- اعلان از SMS دریافتی جدید تولید می‌شود و پیام‌های مسدودشده قبل از Notification کنار گذاشته می‌شوند.
- تنظیمات اعلان هر دسته مستقل از فعال/غیرفعال بودن خود دسته در Inbox است؛ مسیر دریافت زنده برای تعیین مقصد اعلان، Ruleهای موجود را مستقل از فیلتر نمایش دسته‌ها ارزیابی می‌کند.
- برای Android O به بالا، صدای اعلان توسط Notification Channel مدیریت می‌شود؛ تغییر صدا با بازسازی Channel همان دسته اعمال می‌شود.
- برنامه فایل صوتی اختصاصی داخل APK ندارد و از صداهای موجود سیستم استفاده می‌کند.
- **Known Issue:** در تست عملی، انتخاب sound برای category ثبت شده اما صدای Notification پخش نشده است. این مورد در [PRODUCT_FEEDBACK.md](PRODUCT_FEEDBACK.md) ثبت شده و تا تست دستگاه/بررسی Channel نباید بسته تلقی شود.

## امنیت و حریم خصوصی

- برای خواندن و دریافت زنده پیامک، `READ_SMS` و `RECEIVE_SMS` درخواست می‌شوند؛ برای اعلان‌های Android 13+ نیز `POST_NOTIFICATIONS` فقط در زمان نیاز درخواست می‌شود.
- متن پیامک در log چاپ نمی‌شود.
- پیامک‌ها در دیتابیس جداگانه ذخیره نمی‌شوند.
- Ruleها، وضعیت دسته‌ها، تنظیمات فیلتر مسدودسازی و تنظیمات اعلان فقط به‌صورت محلی نگهداری می‌شوند.
- هیچ قابلیت ارسال، حذف یا تغییر SMS در این فاز وجود ندارد.

## خارج از فاز فعلی

- Default SMS App
- ارسال SMS
- حذف یا تغییر SMS
- همگام‌سازی ابری
- حساب کاربری
- ML/LLM classification
- workflowهای پیچیده کاربر

## وضعیت توسعه

این پروژه اکنون در **Improvement Phase** است.

اولویت فعلی:
1. رفع بازخوردها و Bugهای مشاهده‌شده در استفاده واقعی.
2. حفظ کامل قابلیت‌های فعلی از طریق Regression Test.
3. بهبود UX و Performance بدون پیچیده‌کردن غیرضروری پروژه.
4. افزایش دقت Classification با اتکا به نمونه‌های واقعی.
5. سپس اضافه‌کردن قابلیت‌هایی که ارزش واقعی آن‌ها تأیید شده است.

برای جزئیات continuation، baseline و feedback log به این فایل‌ها مراجعه کنید:

- [PROJECT_HANDOFF.md](PROJECT_HANDOFF.md)
- [REGRESSION_BASELINE.md](REGRESSION_BASELINE.md)
- [PRODUCT_FEEDBACK.md](PRODUCT_FEEDBACK.md)

## Git workflow

توسعه قابلیت‌ها روی branchهای feature انجام می‌شود و `main` برای نسخه‌های پایدار نگه داشته می‌شود.

Branch فعلی:

```text
feature/phase-1-sms-inbox
```

Branch فعلی با `main` diverged است؛ قبل از merge باید وضعیت دو branch و commitهای مستقل از `main` بررسی و reconcile شوند.
